"### 7. PDF Alignment & Distortion Fix (Updated)\n- **html2canvas Compatibility Rework**: Fixed a layout bug where the front/back anatomy background images appeared split and distorted in the exported PDF. This was caused by `html2canvas` failing to render the CSS `object-fit: cover` and `object-position: left/right` properties on a side-by-side sprite image.\n- **Cropped Absolute Containers**: Replaced `object-fit` and `object-position` styling with a wrapping layout structure: an outer `div` with `overflow: hidden` containing an absolutely positioned `img` element stretched to `width: 200%`. Using `left: 0` crops to the front view, and `left: -100%` crops to the back view. This standard CSS technique translates perfectly inside the `html2canvas` renderer, resulting in clean, correctly proportioned visual maps in generated PDFs.\n- **Absolute Parent Sizing Guarantee**: Added explicit width and height constraints (`className=\"w-full h-full\" style={{ width: \"100%\", height: \"100%\" }}`) on the absolute container wrapper elements surrounding the image sprites. This prevents browser print engines from collapsing or miscalculating the width of absolute containers (which would lead to circular sizing dependencies with percentage-sized child images inside `aspect` containers).\n- **Inline Image Cropping Attributes**: Configured inline styling values (`width: \"200%\", maxWidth: \"none\", height: \"100%\", left: 0 / \"-100%\"`) directly on the anterior and posterior print `img` tags to ensure correct scaling and alignment is loaded by default even before CSS rules are parsed by user print dialogs.\n- **Print Engine Override Fix**: Resolved a print engine issue where default browser stylesheets (or tailwind preflight) override image dimensions using `img { max-width: 100% !important; }` in print mode. Injected specific custom CSS overrides (`width: 200% !important`, `max-width: none !important`, `height: 100% !important`) targeting `.print-heatmap-card img` and specific `.print-heatmap-card-front` / `.print-heatmap-c
<truncated 1136 bytes>