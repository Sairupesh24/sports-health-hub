generator client {
  provider        = "prisma-client-js"
  output          = "../generated/prisma"
  previewFeatures = ["partialIndexes"]
  engineType      = "library"
}

datasource db {
  provider = "postgresql"
}

model AthleteItemLog {
  id             String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String?       @map("organization_id") @db.Uuid
  workoutItemId  String        @map("workout_item_id") @db.Uuid
  athleteId      String?       @map("athlete_id") @db.Uuid
  loggedAt       DateTime?     @default(now()) @map("logged_at") @db.Timestamptz(6)
  setsCompleted  Json?         @map("sets_completed")
  rpe            Int?
  notes          String?
  skipped        Boolean?      @default(false)
  createdAt      DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  users          User?         @relation(fields: [athleteId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  workoutitems   WorkoutItem   @relation(fields: [workoutItemId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("athlete_item_logs")
}

model AthleteWorkoutCompletion {
  id               String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  athleteId        String?       @map("athlete_id") @db.Uuid
  workoutDayId     String?       @map("workout_day_id") @db.Uuid
  organizationId   String?       @map("organization_id") @db.Uuid
  completedAt      DateTime?     @default(now()) @map("completed_at") @db.Timestamptz(6)
  overallNotes     String?       @map("overall_notes")
  completionStatus String?       @default("completed") @map("completion_status")
  createdAt        DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  users            User?         @relation(fields: [athleteId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations    Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  workoutdays      WorkoutDay?   @relation(fields: [workoutDayId], references: [id], onUpdate: NoAction)

  @@map("athlete_workout_completions")
}

model AuthSession {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  otpCode   String   @map("otp_code")
  expiresAt DateTime @map("expires_at") @db.Timestamptz(6)
  users     User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("authsessions")
}

model AvailabilityException {
  id             String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String?       @map("organization_id") @db.Uuid
  consultantId   String        @map("consultant_id") @db.Uuid
  exceptionDate  DateTime      @map("exception_date") @db.Date
  startTime      DateTime?     @map("start_time") @db.Time(6)
  endTime        DateTime?     @map("end_time") @db.Time(6)
  isBlocked      Boolean?      @default(true) @map("is_blocked")
  reason         String?
  profiles       Profile       @relation(fields: [consultantId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("availabilityexceptions")
}

model BillItem {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  billId         String       @map("bill_id") @db.Uuid
  packageId      String?      @map("package_id") @db.Uuid
  amount         Decimal      @default(0) @db.Decimal
  discount       Decimal?     @default(0) @db.Decimal
  total          Decimal      @default(0) @db.Decimal
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  taxAmount      Decimal?     @default(0) @map("tax_amount") @db.Decimal
  bills          Bill         @relation(fields: [billId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("billitems")
}

model BillPayment {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  billId         String       @map("bill_id") @db.Uuid
  clientId       String?      @map("client_id") @db.Uuid
  amount         Decimal      @db.Decimal
  paymentMethod  String?      @map("payment_method")
  transactionId  String?      @map("transaction_id")
  recordedBy     String?      @map("recorded_by") @db.Uuid
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  bills          Bill         @relation(fields: [billId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  clients        Client?      @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("billpayments")
}

model Bill {
  id                    String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId        String        @map("organization_id") @db.Uuid
  clientId              String        @map("client_id") @db.Uuid
  amount                Decimal       @default(0) @db.Decimal
  discount              Decimal?      @default(0) @db.Decimal
  total                 Decimal       @default(0) @db.Decimal
  status                String        @default("unpaid")
  referralSourceId      String?       @map("referral_source_id") @db.Uuid
  notes                 String?
  includeNotesInInvoice Boolean?      @default(false) @map("include_notes_in_invoice")
  discountAuthorizedBy  String?       @map("discount_authorized_by")
  billedById            String?       @map("billed_by_id") @db.Uuid
  billedByName          String?       @map("billed_by_name")
  billingStaffName      String?       @map("billing_staff_name")
  transactionId         String?       @map("transaction_id")
  paymentMethod         String?       @map("payment_method")
  updatedAt             DateTime?     @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt             DateTime?     @map("deleted_at") @db.Timestamptz(6)
  createdAt             DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  subscriptionId        String?       @map("subscription_id") @db.Uuid
  dueDate               DateTime?     @map("due_date") @db.Date
  taxAmount             Decimal?      @default(0) @map("tax_amount") @db.Decimal
  package_id            String?       @db.Uuid
  date                  DateTime?     @default(dbgenerated("CURRENT_DATE")) @db.Date
  billitems             BillItem[]
  billpayments          BillPayment[]
  users                 User?         @relation(fields: [billedById], references: [id], onDelete: NoAction, onUpdate: NoAction)
  clients               Client        @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations         Organization  @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  packages              Package?      @relation(fields: [package_id], references: [id], onUpdate: NoAction)
  subscriptions         Subscription? @relation(fields: [subscriptionId], references: [id], onUpdate: NoAction)
  refunds               Refund[]

  @@map("bills")
}

model BulkAssignment {
  id              String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId  String         @map("organization_id") @db.Uuid
  questionnaireId String         @map("questionnaire_id") @db.Uuid
  specialistId    String         @map("specialist_id") @db.Uuid
  totalClients    Int            @default(0) @map("total_clients")
  respondedCount  Int            @default(0) @map("responded_count")
  status          String?        @default("active")
  createdAt       DateTime?      @default(now()) @map("created_at") @db.Timestamptz(6)
  organizations   Organization   @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles        Profile        @relation(fields: [specialistId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  formResponses   FormResponse[]

  @@map("bulk_assignments")
}

model ClientAssessmentReport {
  id               String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId   String       @map("organization_id") @db.Uuid
  clientId         String       @map("client_id") @db.Uuid
  title            String
  testIndex        Int          @map("test_index")
  assessmentData   Json         @map("assessment_data")
  reportTexts      Json         @map("report_texts")
  painData         Json         @map("pain_data")
  reassessmentDate String?      @map("reassessment_date")
  createdBy        String?      @map("created_by") @db.Uuid
  createdAt        DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  clients          Client       @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users            User?        @relation(fields: [createdBy], references: [id], onUpdate: NoAction)
  organizations    Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("client_assessment_reports")
}

model ClientGroupMember {
  groupId      String      @map("group_id") @db.Uuid
  clientId     String      @map("client_id") @db.Uuid
  addedBy      String?     @map("added_by") @db.Uuid
  addedAt      DateTime?   @default(now()) @map("added_at") @db.Timestamptz(6)
  users        User?       @relation(fields: [addedBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  clients      Client      @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  clientGroups ClientGroup @relation(fields: [groupId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([groupId, clientId])
  @@map("client_group_members")
}

model ClientGroup {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId     String              @map("organization_id") @db.Uuid
  name               String
  description        String?
  createdBy          String?             @map("created_by") @db.Uuid
  createdAt          DateTime?           @default(now()) @map("created_at") @db.Timestamptz(6)
  clientGroupMembers ClientGroupMember[]
  users              User?               @relation(fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  organizations      Organization        @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organizationId, name])
  @@map("client_groups")
}

model ClientAdminNote {
  clientId  String    @id @map("client_id") @db.Uuid
  remarks   String
  updatedBy String?   @map("updated_by") @db.Uuid
  updatedAt DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients   Client    @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users     User?     @relation(fields: [updatedBy], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("clientadminnotes")
}

model ClientDocument {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId       String       @map("client_id") @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  documentName   String       @map("document_name")
  category       String?
  documentType   String?      @map("document_type")
  filePath       String       @map("file_path")
  uploadedBy     String?      @map("uploaded_by") @db.Uuid
  uploadedByRole String?      @map("uploaded_by_role")
  notes          String?
  accessLevel    String?      @default("Medical_Staff_Only") @map("access_level")
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  clients        Client       @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users          User?        @relation(fields: [uploadedBy], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("clientdocuments")
}

model ClientEntitlement {
  id              String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId  String       @map("organization_id") @db.Uuid
  clientId        String       @map("client_id") @db.Uuid
  invoiceId       String?      @map("invoice_id") @db.Uuid
  packageId       String?      @map("package_id") @db.Uuid
  serviceId       String?      @map("service_id") @db.Uuid
  serviceType     String       @map("service_type")
  grantedSessions Int          @map("granted_sessions")
  sessionsUsed    Int?         @default(0) @map("sessions_used")
  status          String?      @default("active")
  billItemId      String?      @map("bill_item_id") @db.Uuid
  notes           String?
  createdAt       DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime?    @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients         Client       @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  packages        Package?     @relation(fields: [packageId], references: [id], onUpdate: NoAction)
  services        Service?     @relation(fields: [serviceId], references: [id], onUpdate: NoAction)
  sessions        Session[]

  @@map("cliententitlements")
}

model ClientFieldConfig {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  fieldName      String       @map("field_name")
  isMandatory    Boolean      @default(false) @map("is_mandatory")
  updatedAt      DateTime     @default(now()) @map("updated_at") @db.Timestamptz(6)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organizationId, fieldName], map: "client_field_config_organization_id_field_name_key")
  @@map("client_field_config")
}

model ClientOrganization {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  name           String
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organizationId, name])
  @@map("clientorganizations")
}

model Client {
  id                                          String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId                              String                    @map("organization_id") @db.Uuid
  locationId                                  String?                   @map("location_id") @db.Uuid
  uhid                                        String                    @unique
  registeredOn                                DateTime?                 @default(now()) @map("registered_on") @db.Timestamptz(6)
  honorific                                   String?
  firstName                                   String                    @map("first_name")
  middleName                                  String?                   @map("middle_name")
  lastName                                    String                    @map("last_name")
  gender                                      String?
  mobileNo                                    String                    @map("mobile_no")
  aadhaarNo                                   String?                   @map("aadhaar_no")
  bloodGroup                                  String?                   @map("blood_group")
  dob                                         DateTime?                 @db.Date
  age                                         Int?
  email                                       String?
  alternateMobileNo                           String?                   @map("alternate_mobile_no")
  occupation                                  String?
  sport                                       String?
  athleteType                                 String?                   @map("athlete_type")
  orgName                                     String?                   @map("org_name")
  address                                     String?
  locality                                    String?
  pincode                                     String?
  city                                        String?
  district                                    String?
  state                                       String?
  country                                     String?                   @default("India")
  hasInsurance                                Boolean?                  @default(false) @map("has_insurance")
  insuranceProvider                           String?                   @map("insurance_provider")
  insurancePolicyNo                           String?                   @map("insurance_policy_no")
  insuranceValidity                           DateTime?                 @map("insurance_validity") @db.Date
  insuranceCoverageAmount                     Decimal?                  @map("insurance_coverage_amount") @db.Decimal
  isVip                                       Boolean?                  @default(false) @map("is_vip")
  referralSource                              String?                   @map("referral_source")
  referralSourceDetail                        String?                   @map("referral_source_detail")
  assignedConsultantId                        String?                   @map("assigned_consultant_id") @db.Uuid
  createdBy                                   String?                   @map("created_by") @db.Uuid
  createdAt                                   DateTime?                 @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                                   DateTime?                 @default(now()) @map("updated_at") @db.Timestamptz(6)
  profileId                                   String?                   @map("profile_id") @db.Uuid
  primaryScientistId                          String?                   @map("primary_scientist_id") @db.Uuid
  deletedAt                                   DateTime?                 @map("deleted_at") @db.Timestamptz(6)
  billpayments                                BillPayment[]
  bills                                       Bill[]
  clientAssessmentReports                     ClientAssessmentReport[]
  clientGroupMembers                          ClientGroupMember[]
  clientadminnotes                            ClientAdminNote?
  clientdocuments                             ClientDocument[]
  cliententitlements                          ClientEntitlement[]
  usersClientsAssignedConsultantIdTousers     User?                     @relation("clients_assigned_consultant_idTousers", fields: [assignedConsultantId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  usersClientsCreatedByTousers                User?                     @relation("clients_created_byTousers", fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  locations                                   Location?                 @relation(fields: [locationId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  organizations                               Organization              @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profilesClientsPrimaryScientistIdToprofiles Profile?                  @relation("clients_primary_scientist_idToprofiles", fields: [primaryScientistId], references: [id], onUpdate: NoAction)
  profilesClientsProfileIdToprofiles          Profile?                  @relation("clients_profile_idToprofiles", fields: [profileId], references: [id], onUpdate: NoAction)
  enquiries                                   Enquiry[]
  externalTrainingSummary                     ExternalTrainingSummary[]
  formResponses                               FormResponse[]
  groupAttendance                             GroupAttendance[]
  injuries                                    Injury[]
  nutritionAssessments                        NutritionAssessment[]
  performanceAssessments                      PerformanceAssessment[]
  refunds                                     Refund[]
  scientificResources                         ScientificResource[]
  sessions                                    Session[]
  subscriptions                               Subscription[]
  waitlist                                    Waitlist[]

  @@map("clients")
}

model ConsultantService {
  organizationId String       @map("organization_id") @db.Uuid
  consultantId   String       @map("consultant_id") @db.Uuid
  serviceId      String       @map("service_id") @db.Uuid
  profiles       Profile      @relation(fields: [consultantId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  services       Service      @relation(fields: [serviceId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([consultantId, serviceId])
  @@map("consultant_services")
}

model ConsultantAvailability {
  id                   String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId       String?       @map("organization_id") @db.Uuid
  consultantId         String        @map("consultant_id") @db.Uuid
  dayOfWeek            Int           @map("day_of_week")
  startTime            DateTime      @map("start_time") @db.Time(6)
  endTime              DateTime      @map("end_time") @db.Time(6)
  slotDurationInterval Int?          @default(30) @map("slot_duration_interval")
  bufferTime           Int?          @default(0) @map("buffer_time")
  profiles             Profile       @relation(fields: [consultantId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations        Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([consultantId, dayOfWeek])
  @@map("consultantavailability")
}

model EmergencyAlert {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  staffId        String       @map("staff_id") @db.Uuid
  status         String?      @default("unresolved")
  reason         String?
  latitude       Decimal?     @db.Decimal
  longitude      Decimal?     @db.Decimal
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  admin_decision String?
  updated_at     DateTime?    @default(now()) @db.Timestamptz(6)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles       Profile      @relation(fields: [staffId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("emergency_alerts")
}

model Enquiry {
  id                  String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId      String               @map("organization_id") @db.Uuid
  firstName           String               @map("first_name")
  lastName            String?              @map("last_name")
  mobileNo            String               @map("mobile_no")
  email               String?
  lookingFor          String?              @map("looking_for")
  preferredCallTime   String?              @map("preferred_call_time")
  referralSource      String?              @map("referral_source")
  referralDetails     String?              @map("referral_details")
  workPlace           String?              @map("work_place")
  notes               String?
  status              String?              @default("pending")
  linkedClientId      String?              @map("linked_client_id") @db.Uuid
  nextFollowUpAt      DateTime?            @map("next_follow_up_at") @db.Timestamptz(6)
  lastInteractionAt   DateTime?            @map("last_interaction_at") @db.Timestamptz(6)
  createdAt           DateTime?            @default(now()) @map("created_at") @db.Timestamptz(6)
  clients             Client?              @relation(fields: [linkedClientId], references: [id], onUpdate: NoAction)
  organizations       Organization         @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enquiryinteractions EnquiryInteraction[]
  sessions            Session[]

  @@map("enquiries")
}

model EnquiryInteraction {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  enquiryId        String    @map("enquiry_id") @db.Uuid
  interactionType  String    @map("interaction_type")
  responseText     String?   @map("response_text")
  followUpRequired Boolean?  @default(false) @map("follow_up_required")
  followUpAt       DateTime? @map("follow_up_at") @db.Timestamptz(6)
  createdBy        String?   @map("created_by") @db.Uuid
  createdAt        DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  users            User?     @relation(fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  enquiries        Enquiry   @relation(fields: [enquiryId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("enquiryinteractions")
}

model ExcelDiagnosticReport {
  id                     String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId         String       @map("organization_id") @db.Uuid
  clientUhid             String?      @map("client_uhid")
  patientName            String       @map("patient_name")
  dob                    DateTime?    @db.Date
  testHistory            Json         @map("test_history")
  latestMetrics          Json         @map("latest_metrics")
  clinicalInterpretation String?      @map("clinical_interpretation")
  createdBy              String?      @map("created_by") @db.Uuid
  createdAt              DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  users                  User?        @relation(fields: [createdBy], references: [id], onUpdate: NoAction)
  organizations          Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("excel_diagnostic_reports")
}

model Exercise {
  id                String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId    String?       @map("organization_id") @db.Uuid
  name              String
  description       String?
  category          String?
  equipmentType     String?       @map("equipment_type")
  difficultyLevel   String?       @map("difficulty_level")
  muscleGroups      String[]      @map("muscle_groups")
  bodyRegion        String?       @map("body_region")
  equipmentRequired String?       @map("equipment_required")
  instructions      String?
  videoUrl          String?       @map("video_url")
  isRehabilitation  Boolean?      @default(false) @map("is_rehabilitation")
  isActive          Boolean?      @default(true) @map("is_active")
  createdAt         DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime?     @default(now()) @map("updated_at") @db.Timestamptz(6)
  organizations     Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  liftitems         LiftItem[]
  maxPrRecords      MaxPrRecord[]

  @@unique([organizationId, name])
  @@map("exercises")
}

model ExternalTrainingSummary {
  id               String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId   String       @map("organization_id") @db.Uuid
  clientId         String       @map("client_id") @db.Uuid
  externalSystem   String       @default("TeamBuildr") @map("external_system")
  trainingDate     DateTime     @map("training_date") @db.Date
  workoutName      String?      @map("workout_name")
  durationMinutes  Int?         @map("duration_minutes")
  trainingLoad     Decimal?     @map("training_load") @db.Decimal
  completionStatus String?      @map("completion_status")
  createdAt        DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime?    @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients          Client       @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations    Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([clientId, trainingDate(sort: Desc)], map: "idx_ext_training_client_date")
  @@map("external_training_summary")
}

model FormResponse {
  id                     String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId         String          @map("organization_id") @db.Uuid
  formId                 String          @map("form_id") @db.Uuid
  clientId               String          @map("client_id") @db.Uuid
  specialistId           String?         @map("specialist_id") @db.Uuid
  bulkAssignmentId       String?         @map("bulk_assignment_id") @db.Uuid
  status                 String?         @default("pending")
  responseData           Json?           @map("response_data")
  clinicalInterpretation String?         @map("clinical_interpretation")
  submittedAt            DateTime?       @map("submitted_at") @db.Timestamptz(6)
  createdAt              DateTime?       @default(now()) @map("created_at") @db.Timestamptz(6)
  bulkAssignments        BulkAssignment? @relation(fields: [bulkAssignmentId], references: [id], onUpdate: NoAction)
  clients                Client          @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations          Organization    @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles               Profile?        @relation(fields: [specialistId], references: [id], onUpdate: NoAction)

  @@map("form_responses")
}

model GroupAttendance {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionId        String    @map("session_id") @db.Uuid
  clientId         String    @map("client_id") @db.Uuid
  attendanceStatus String    @default("Present") @map("attendance_status")
  notes            String?
  createdAt        DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  clients          Client    @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sessions         Session   @relation(fields: [sessionId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([sessionId, clientId])
  @@map("group_attendance")
}

model HrEmployee {
  id                    String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId        String       @map("organization_id") @db.Uuid
  profileId             String       @unique @map("profile_id") @db.Uuid
  jobId                 String?      @map("job_id") @db.Uuid
  dateOfJoining         DateTime?    @map("date_of_joining") @db.Date
  employmentType        String?      @map("employment_type")
  bankName              String?      @map("bank_name")
  bankAccountNo         String?      @map("bank_account_no")
  ifscCode              String?      @map("ifsc_code")
  emergencyContactName  String?      @map("emergency_contact_name")
  emergencyContactPhone String?      @map("emergency_contact_phone")
  createdAt             DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  hrJobs                HrJob?       @relation(fields: [jobId], references: [id], onUpdate: NoAction)
  organizations         Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles              Profile      @relation(fields: [profileId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("hr_employees")
}

model HrJob {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  name           String
  description    String?
  isActive       Boolean?     @default(true) @map("is_active")
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  hrEmployees    HrEmployee[]
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("hr_jobs")
}

model HrAttendanceLog {
  id                 String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId     String       @map("organization_id") @db.Uuid
  profileId          String       @map("profile_id") @db.Uuid
  type               String
  latitude           Decimal?     @db.Decimal
  longitude          Decimal?     @db.Decimal
  distanceFromCenter Decimal?     @map("distance_from_center") @db.Decimal
  isWithinGeofence   Boolean?     @default(true) @map("is_within_geofence")
  metadata           Json?
  createdAt          DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  remark             String?
  organizations      Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles           Profile      @relation(fields: [profileId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("hrattendancelogs")
}

model HrLeave {
  id                                   String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId                       String       @map("organization_id") @db.Uuid
  employeeId                           String       @map("employee_id") @db.Uuid
  leaveType                            String       @map("leave_type")
  startDate                            DateTime     @map("start_date") @db.Date
  endDate                              DateTime     @map("end_date") @db.Date
  reason                               String?
  status                               String?      @default("Requested")
  approvedBy                           String?      @map("approved_by") @db.Uuid
  createdAt                            DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  profilesHrleavesApprovedByToprofiles Profile?     @relation("hrleaves_approved_byToprofiles", fields: [approvedBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profilesHrleavesEmployeeIdToprofiles Profile      @relation("hrleaves_employee_idToprofiles", fields: [employeeId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations                        Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("hrleaves")
}

model Injury {
  id             String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String          @map("organization_id") @db.Uuid
  clientId       String          @map("client_id") @db.Uuid
  diagnosis      String
  injuryType     String          @map("injury_type")
  region         String
  injuryDate     DateTime?       @default(now()) @map("injury_date") @db.Timestamptz(6)
  status         String          @default("Active")
  side           String?
  onset          String?
  mechanism      String?
  notes          String?
  createdAt      DateTime?       @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt      DateTime?       @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients        Client          @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization    @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rehabProgress  RehabProgress[]

  @@map("injuries")
}

model InjuryMasterData {
  id             String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String?       @map("organization_id") @db.Uuid
  region         String
  injuryType     String        @map("injury_type")
  diagnosis      String
  createdAt      DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  organizations  Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([region, injuryType, diagnosis], map: "injury_master_data_global_unique_idx", where: raw("(organization_id IS NULL)"))
  @@unique([organizationId, region, injuryType, diagnosis], map: "injury_master_data_org_unique_idx", where: raw("(organization_id IS NOT NULL)"))
  @@map("injury_master_data")
}

model LiftItem {
  id              String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workoutItemId   String      @map("workout_item_id") @db.Uuid
  exerciseId      String      @map("exercise_id") @db.Uuid
  sets            Int         @default(1)
  reps            String?
  loadType        String?     @default("kg") @map("load_type")
  loadValue       Decimal?    @map("load_value") @db.Decimal
  tempo           String?
  restTimeSecs    Int?        @map("rest_time_secs")
  additionalInfo  String?     @map("additional_info")
  workoutGrouping String?     @map("workout_grouping")
  exercises       Exercise    @relation(fields: [exerciseId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  workoutitems    WorkoutItem @relation(fields: [workoutItemId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("liftitems")
}

model Location {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  name           String
  address        String?
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt      DateTime?    @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients        Client[]
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("locations")
}

model MaxPrRecord {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  athleteId  String?   @map("athlete_id") @db.Uuid
  exerciseId String?   @map("exercise_id") @db.Uuid
  value      Decimal   @db.Decimal
  isCurrent  Boolean?  @default(true) @map("is_current")
  recordedAt DateTime? @default(now()) @map("recorded_at") @db.Timestamptz(6)
  createdAt  DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  users      User?     @relation(fields: [athleteId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  exercises  Exercise? @relation(fields: [exerciseId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("max_pr_records")
}

model NotificationRead {
  notificationId String       @map("notification_id") @db.Uuid
  userId         String       @map("user_id") @db.Uuid
  readAt         DateTime?    @default(now()) @map("read_at") @db.Timestamptz(6)
  notifications  Notification @relation(fields: [notificationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users          User         @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([notificationId, userId])
  @@map("notification_reads")
}

model Notification {
  id                String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId    String             @map("organization_id") @db.Uuid
  title             String
  content           String
  type              String?            @default("info")
  targetRole        String?            @map("target_role")
  targetUserId      String?            @map("target_user_id") @db.Uuid
  isBroadcast       Boolean?           @default(false) @map("is_broadcast")
  priority          String?            @default("normal")
  createdBy         String?            @map("created_by") @db.Uuid
  createdAt         DateTime?          @default(now()) @map("created_at") @db.Timestamptz(6)
  category          String?            @default("in_app")
  actionPayload     Json?              @default("{}") @map("action_payload")
  actionStatus      String?            @default("pending") @map("action_status")
  isVip             Boolean?           @default(false) @map("is_vip")
  senderId          String?            @map("sender_id") @db.Uuid
  notificationReads NotificationRead[]
  users             User?              @relation(fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  organizations     Organization       @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles          Profile?           @relation(fields: [senderId], references: [id], onUpdate: NoAction)

  @@map("notifications")
}

model OrganizationNotificationSetting {
  organizationId               String       @id @map("organization_id") @db.Uuid
  enableEmailNotifications     Boolean?     @default(true) @map("enable_email_notifications")
  enableInAppNotifications     Boolean?     @default(true) @map("enable_in_app_notifications")
  notifySignupApproval         Boolean?     @default(true) @map("notify_signup_approval")
  notifyQuestionnaireAssigned  Boolean?     @default(true) @map("notify_questionnaire_assigned")
  notifyQuestionnaireCompleted Boolean?     @default(true) @map("notify_questionnaire_completed")
  notifyEmergencyLeave         Boolean?     @default(true) @map("notify_emergency_leave")
  notifyOutstandingBalance     Boolean?     @default(true) @map("notify_outstanding_balance")
  updatedAt                    DateTime?    @default(now()) @map("updated_at") @db.Timestamptz(6)
  organizations                Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("organization_notification_settings")
}

model Organization {
  id                               String                           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                             String
  createdAt                        DateTime?                        @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                        DateTime?                        @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt                        DateTime?                        @map("deleted_at") @db.Timestamptz(6)
  orgCode                          String?                          @map("org_code")
  slug                             String?
  subscriptionPlan                 String?                          @default("pro") @map("subscription_plan")
  status                           String?                          @default("active")
  uhidPrefix                       String?                          @map("uhid_prefix")
  logoUrl                          String?                          @map("logo_url")
  officialName                     String?                          @map("official_name")
  officialAddress                  String?                          @map("official_address")
  contactEmail                     String?                          @map("contact_email")
  contactPhone                     String?                          @map("contact_phone")
  clinicLatitude                   Decimal?                         @map("clinic_latitude") @db.Decimal
  clinicLongitude                  Decimal?                         @map("clinic_longitude") @db.Decimal
  geofenceRadius                   Decimal?                         @map("geofence_radius") @db.Decimal
  enableGeofencing                 Boolean?                         @default(false) @map("enable_geofencing")
  enableIpLocking                  Boolean?                         @default(false) @map("enable_ip_locking")
  allowedIps                       String?                          @map("allowed_ips")
  allowCustomDuration              Boolean?                         @default(false) @map("allow_custom_duration")
  defaultSlotDuration              Int?                             @default(60) @map("default_slot_duration")
  defaultSlotCapacity              Int?                             @default(2) @map("default_slot_capacity")
  customSpecialistSettings         Json?                            @default("{}") @map("custom_specialist_settings")
  defaultCheckoutTime              DateTime?                        @default(dbgenerated("'18:00:00'::time without time zone")) @map("default_checkout_time") @db.Time(6)
  default_shift_end_time           DateTime?                        @default(dbgenerated("'18:00:00'::time without time zone")) @db.Time(6)
  enabledModules                   String?                          @map("enabled_modules")
  activityTrackerAutomations       ActivityTrackerAutomation[]
  athleteItemLogs                  AthleteItemLog[]
  athleteWorkoutCompletions        AthleteWorkoutCompletion[]
  audit_logs                       audit_logs[]
  availabilityexceptions           AvailabilityException[]
  billitems                        BillItem[]
  billpayments                     BillPayment[]
  bills                            Bill[]
  bulkAssignments                  BulkAssignment[]
  clientAssessmentReports          ClientAssessmentReport[]
  clientFieldConfigs               ClientFieldConfig[]
  clientGroups                     ClientGroup[]
  clientdocuments                  ClientDocument[]
  cliententitlements               ClientEntitlement[]
  clientorganizations              ClientOrganization[]
  clients                          Client[]
  consultantServices               ConsultantService[]
  consultantavailability           ConsultantAvailability[]
  emergencyAlerts                  EmergencyAlert[]
  enquiries                        Enquiry[]
  excelDiagnosticReports           ExcelDiagnosticReport[]
  exercises                        Exercise[]
  externalTrainingSummary          ExternalTrainingSummary[]
  formResponses                    FormResponse[]
  hrEmployees                      HrEmployee[]
  hrJobs                           HrJob[]
  hr_leave_balances                hr_leave_balances[]
  hrattendancelogs                 HrAttendanceLog[]
  hrleaves                         HrLeave[]
  injuries                         Injury[]
  injuryMasterData                 InjuryMasterData[]
  locations                        Location[]
  notifications                    Notification[]
  nutritionAssessments             NutritionAssessment[]
  organizationNotificationSettings OrganizationNotificationSetting?
  packages                         Package[]
  performanceAssessments           PerformanceAssessment[]
  profiles                         Profile[]
  program_assignments              ProgramAssignment[]
  questionnaires                   Questionnaire[]
  recurring_questionnaires         recurring_questionnaires[]
  referralsources                  ReferralSource[]
  refunds                          Refund[]
  reportTemplates                  ReportTemplate[]
  scientificResources              ScientificResource[]
  services                         Service[]
  sessions                         Session[]
  staffSchedules                   StaffSchedule[]
  subscriptionLogs                 SubscriptionLog[]
  subscriptions                    Subscription[]
  trainingprograms                 TrainingProgram[]
  uhidsequences                    UhidSequence[]
  waitlist                         Waitlist[]
  wellnessLogs                     WellnessLog[]
  workoutdays                      WorkoutDay[]

  @@map("organizations")
}

model Package {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId     String              @map("organization_id") @db.Uuid
  name               String
  description        String?
  price              Decimal             @default(0) @db.Decimal
  deletedAt          DateTime?           @map("deleted_at") @db.Timestamptz(6)
  createdAt          DateTime?           @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime?           @default(now()) @map("updated_at") @db.Timestamptz(6)
  category           String?             @default("Others")
  tax_percentage     Decimal             @default(0) @db.Decimal
  taxAmount          Decimal             @default(0) @map("tax_amount") @db.Decimal
  bills              Bill[]
  cliententitlements ClientEntitlement[]
  organizations      Organization        @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  packageservices    PackageService[]
  subscriptions      Subscription[]

  @@unique([organizationId, name])
  @@map("packages")
}

model PackageService {
  id               String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  packageId        String  @map("package_id") @db.Uuid
  serviceId        String  @map("service_id") @db.Uuid
  sessionsIncluded Int     @default(1) @map("sessions_included")
  packages         Package @relation(fields: [packageId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  services         Service @relation(fields: [serviceId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("packageservices")
}

model PerformanceAssessment {
  id             String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String?       @map("organization_id") @db.Uuid
  athleteId      String        @map("athlete_id") @db.Uuid
  category       String
  testName       String        @map("test_name")
  metrics        Json          @default("{}")
  recordedBy     String?       @map("recorded_by") @db.Uuid
  createdAt      DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  clients        Client        @relation(fields: [athleteId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles       Profile?      @relation(fields: [recordedBy], references: [id], onUpdate: NoAction)

  @@map("performance_assessments")
}

model PhysioSessionDetail {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionId        String?   @unique @map("session_id") @db.Uuid
  injuryId         String?   @map("injury_id") @db.Uuid
  painScore        Int?      @map("pain_score")
  modalityUsed     String?   @map("modality_used")
  treatmentType    String?   @map("treatment_type")
  manualTherapy    String?   @map("manual_therapy")
  exerciseGiven    String?   @map("exercise_given")
  rangeOfMotion    String?   @map("range_of_motion")
  strengthProgress String?   @map("strength_progress")
  clinicalNotes    String?   @map("clinical_notes")
  nextPlan         String?   @map("next_plan")
  sorenessData     Json?     @map("soreness_data")
  updatedAt        DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)
  sessions         Session?  @relation(fields: [sessionId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("physiosessiondetails")
}

model Profile {
  id                                         String                     @id @db.Uuid
  firstName                                  String                     @map("first_name")
  lastName                                   String                     @map("last_name")
  organizationId                             String?                    @map("organization_id") @db.Uuid
  isApproved                                 Boolean?                   @default(false) @map("is_approved")
  uhid                                       String?
  amsRole                                    String?                    @map("ams_role")
  profession                                 String?
  createdAt                                  DateTime?                  @default(now()) @map("created_at") @db.Timestamptz(6)
  avatarUrl                                  String?                    @map("avatar_url")
  mobileNo                                   String?                    @map("mobile_no")
  hasCalendarAccess                          Boolean?                   @default(false) @map("has_calendar_access")
  hasAnalyticsAccess                         Boolean?                   @default(false) @map("has_analytics_access")
  hasAssignWorkAccess                        Boolean?                   @default(false) @map("has_assign_work_access")
  allowedConsoles                            String?                    @map("allowed_consoles")
  availabilityexceptions                     AvailabilityException[]
  bulkAssignments                            BulkAssignment[]
  clientsClientsPrimaryScientistIdToprofiles Client[]                   @relation("clients_primary_scientist_idToprofiles")
  clientsClientsProfileIdToprofiles          Client[]                   @relation("clients_profile_idToprofiles")
  consultantServices                         ConsultantService[]
  consultantavailability                     ConsultantAvailability[]
  emergencyAlerts                            EmergencyAlert[]
  formResponses                              FormResponse[]
  hrEmployees                                HrEmployee?
  hr_leave_balances                          hr_leave_balances[]
  hrattendancelogs                           HrAttendanceLog[]
  hrleavesHrleavesApprovedByToprofiles       HrLeave[]                  @relation("hrleaves_approved_byToprofiles")
  hrleavesHrleavesEmployeeIdToprofiles       HrLeave[]                  @relation("hrleaves_employee_idToprofiles")
  notifications                              Notification[]
  performanceAssessments                     PerformanceAssessment[]
  users                                      User                       @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations                              Organization?              @relation(fields: [organizationId], references: [id], onUpdate: NoAction)
  recurring_questionnaires                   recurring_questionnaires[]
  rehabProgress                              RehabProgress[]
  sessionTemplates                           SessionTemplate[]
  sessionsSessionsScientistIdToprofiles      Session[]                  @relation("sessions_scientist_idToprofiles")
  sessionsSessionsTherapistIdToprofiles      Session[]                  @relation("sessions_therapist_idToprofiles")
  staffSchedules                             StaffSchedule?
  trainingprograms                           TrainingProgram[]
  waitlist                                   Waitlist[]

  @@map("profiles")
}

model ProgramAssignment {
  id               String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  athleteId        String?         @map("athlete_id") @db.Uuid
  programId        String          @map("program_id") @db.Uuid
  batchId          String?         @map("batch_id") @db.Uuid
  startDate        DateTime        @map("start_date") @db.Date
  status           String?         @default("active")
  organization_id  String?         @db.Uuid
  createdAt        DateTime?       @default(now()) @map("created_at") @db.Timestamptz(6)
  users            User?           @relation(fields: [athleteId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations    Organization?   @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  trainingprograms TrainingProgram @relation(fields: [programId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("program_assignments")
}

model Questionnaire {
  id                       String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId           String                     @map("organization_id") @db.Uuid
  name                     String
  classification           String?                    @default("performance")
  questions                Json                       @default("[]")
  createdBy                String?                    @map("created_by") @db.Uuid
  createdAt                DateTime?                  @default(now()) @map("created_at") @db.Timestamptz(6)
  users                    User?                      @relation(fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  organizations            Organization               @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  recurring_questionnaires recurring_questionnaires[]

  @@map("questionnaires")
}

model ReferralSource {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  name           String
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organizationId, name])
  @@map("referralsources")
}

model Refund {
  id                    String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId        String       @map("organization_id") @db.Uuid
  clientId              String       @map("client_id") @db.Uuid
  billId                String?      @map("bill_id") @db.Uuid
  amount                Decimal      @db.Decimal
  refundMode            String?      @map("refund_mode")
  transactionId         String?      @map("transaction_id")
  refundProofUrl        String?      @map("refund_proof_url")
  reason                String?
  notes                 String?
  status                String?      @default("pending")
  isOverride            Boolean?     @default(false) @map("is_override")
  authorizedBy          String?      @map("authorized_by")
  isEntitlementReversed Boolean?     @default(false) @map("is_entitlement_reversed")
  createdAt             DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  bills                 Bill?        @relation(fields: [billId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  clients               Client       @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations         Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("refunds")
}

model RehabProgress {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  injuryId   String    @map("injury_id") @db.Uuid
  status     String
  notes      String?
  metrics    Json?     @default("{}")
  recordedBy String?   @map("recorded_by") @db.Uuid
  createdAt  DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  injuries   Injury    @relation(fields: [injuryId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles   Profile?  @relation(fields: [recordedBy], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("rehab_progress")
}

model ReportTemplate {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  name           String
  filePath       String       @map("file_path")
  description    String?
  createdBy      String?      @map("created_by") @db.Uuid
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  users          User?        @relation(fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("report_templates")
}

model ScientificResource {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  athleteId      String?      @map("athlete_id") @db.Uuid
  title          String
  category       String
  type           String
  description    String?
  url            String
  thumbnailUrl   String?      @map("thumbnail_url")
  tags           String[]
  isPublic       Boolean?     @default(false) @map("is_public")
  createdBy      String?      @map("created_by") @db.Uuid
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt      DateTime?    @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients        Client?      @relation(fields: [athleteId], references: [id], onUpdate: NoAction)
  users          User?        @relation(fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("scientific_resources")
}

model Service {
  id                                      String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId                          String              @map("organization_id") @db.Uuid
  name                                    String
  category                                String?             @default("General")
  isActive                                Boolean?            @default(true) @map("is_active")
  createdAt                               DateTime?           @default(now()) @map("created_at") @db.Timestamptz(6)
  basePrice                               Decimal?            @default(0) @map("base_price") @db.Decimal
  minDuration                             Int?                @default(30) @map("min_duration")
  maxDuration                             Int?                @default(120) @map("max_duration")
  isUniversal                             Boolean?            @default(true) @map("is_universal")
  cliententitlements                      ClientEntitlement[]
  consultantServices                      ConsultantService[]
  packageservices                         PackageService[]
  organizations                           Organization        @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sessionTemplates                        SessionTemplate[]
  sessionsSessionsServiceIdToservices     Session[]           @relation("sessions_service_idToservices")
  sessionsSessionsSessionTypeIdToservices Session[]           @relation("sessions_session_type_idToservices")
  waitlist                                Waitlist[]

  @@map("services")
}

model SessionTemplate {
  id              String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  scientistId     String                   @map("scientist_id") @db.Uuid
  templateName    String                   @map("template_name")
  sessionTypeId   String?                  @map("session_type_id") @db.Uuid
  defaultDuration Unsupported("interval")? @default(dbgenerated("'01:00:00'::interval")) @map("default_duration")
  notes           String?
  createdAt       DateTime?                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime?                @default(now()) @map("updated_at") @db.Timestamptz(6)
  profiles        Profile                  @relation(fields: [scientistId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  services        Service?                 @relation(fields: [sessionTypeId], references: [id], onUpdate: NoAction)

  @@map("session_templates")
}

model Session {
  id                                      String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId                          String               @map("organization_id") @db.Uuid
  clientId                                String?              @map("client_id") @db.Uuid
  therapistId                             String?              @map("therapist_id") @db.Uuid
  scientistId                             String?              @map("scientist_id") @db.Uuid
  entitlementId                           String?              @map("entitlement_id") @db.Uuid
  serviceId                               String?              @map("service_id") @db.Uuid
  serviceType                             String               @map("service_type")
  sessionMode                             String?              @default("Individual") @map("session_mode")
  scheduledStart                          DateTime             @map("scheduled_start") @db.Timestamptz(6)
  scheduledEnd                            DateTime             @map("scheduled_end") @db.Timestamptz(6)
  actualStart                             DateTime?            @map("actual_start") @db.Timestamptz(6)
  actualEnd                               DateTime?            @map("actual_end") @db.Timestamptz(6)
  status                                  String               @default("Planned")
  cancellationReason                      String?              @map("cancellation_reason")
  isUnentitled                            Boolean?             @default(false) @map("is_unentitled")
  preferenceType                          String?              @default("Strict") @map("preference_type")
  isFlexibleRouting                       Boolean?             @default(false) @map("is_flexible_routing")
  createdBy                               String?              @map("created_by") @db.Uuid
  createdAt                               DateTime?            @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                               DateTime?            @default(now()) @map("updated_at") @db.Timestamptz(6)
  groupName                               String?              @map("group_name")
  sessionLocation                         String?              @map("session_location")
  sessionNotes                            String?              @map("session_notes")
  attachments                             Json?                @default("[]")
  sessionTypeId                           String?              @map("session_type_id") @db.Uuid
  is_guest                                Boolean?             @default(false)
  guest_name                              String?
  guest_contact                           String?
  enquiry_id                              String?              @db.Uuid
  rescheduledFromSessionId                String?              @map("rescheduled_from_session_id") @db.Uuid
  rescheduledToSessionId                  String?              @map("rescheduled_to_session_id") @db.Uuid
  groupAttendance                         GroupAttendance[]
  physiosessiondetails                    PhysioSessionDetail?
  clients                                 Client?              @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                                   User?                @relation(fields: [createdBy], references: [id], onDelete: NoAction, onUpdate: NoAction)
  enquiries                               Enquiry?             @relation(fields: [enquiry_id], references: [id], onUpdate: NoAction)
  cliententitlements                      ClientEntitlement?   @relation(fields: [entitlementId], references: [id], onUpdate: NoAction)
  organizations                           Organization         @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rescheduledFromSession                  Session?             @relation("SessionRescheduledFrom", fields: [rescheduledFromSessionId], references: [id], onUpdate: NoAction)
  rescheduledToSessions                   Session[]            @relation("SessionRescheduledFrom")
  rescheduledToSession                    Session?             @relation("SessionRescheduledTo", fields: [rescheduledToSessionId], references: [id], onUpdate: NoAction)
  rescheduledFromSessions                 Session[]            @relation("SessionRescheduledTo")
  profilesSessionsScientistIdToprofiles   Profile?             @relation("sessions_scientist_idToprofiles", fields: [scientistId], references: [id], onUpdate: NoAction)
  servicesSessionsServiceIdToservices     Service?             @relation("sessions_service_idToservices", fields: [serviceId], references: [id], onUpdate: NoAction)
  servicesSessionsSessionTypeIdToservices Service?             @relation("sessions_session_type_idToservices", fields: [sessionTypeId], references: [id], onUpdate: NoAction)
  profilesSessionsTherapistIdToprofiles   Profile?             @relation("sessions_therapist_idToprofiles", fields: [therapistId], references: [id], onUpdate: NoAction)

  @@map("sessions")
}

model StaffSchedule {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  consultantId   String       @unique @map("consultant_id") @db.Uuid
  shiftStart     DateTime     @default(dbgenerated("'08:00:00'::time without time zone")) @map("shift_start") @db.Time(6)
  shiftEnd       DateTime     @default(dbgenerated("'17:00:00'::time without time zone")) @map("shift_end") @db.Time(6)
  breaks         Json         @default("[]")
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt      DateTime?    @default(now()) @map("updated_at") @db.Timestamptz(6)
  profiles       Profile      @relation(fields: [consultantId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("staff_schedules")
}

model SubscriptionLog {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  subscriptionId String       @map("subscription_id") @db.Uuid
  event          String
  details        Json?        @default("{}")
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subscriptions  Subscription @relation(fields: [subscriptionId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("subscription_logs")
}

model Subscription {
  id                 String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId     String            @map("organization_id") @db.Uuid
  clientId           String            @map("client_id") @db.Uuid
  packageId          String            @map("package_id") @db.Uuid
  status             String            @default("Active")
  currentPeriodStart DateTime          @default(dbgenerated("CURRENT_DATE")) @map("current_period_start") @db.Date
  currentPeriodEnd   DateTime?         @map("current_period_end") @db.Date
  billingCycle       String            @map("billing_cycle")
  autoPay            Boolean?          @default(false) @map("auto_pay")
  nextBillingDate    DateTime?         @map("next_billing_date") @db.Date
  gracePeriodEnd     DateTime?         @map("grace_period_end") @db.Date
  cancelAtPeriodEnd  Boolean?          @default(false) @map("cancel_at_period_end")
  createdAt          DateTime?         @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime?         @default(now()) @map("updated_at") @db.Timestamptz(6)
  dunningStep        Int?              @default(0) @map("dunning_step")
  lastDunningAt      DateTime?         @map("last_dunning_at") @db.Timestamptz(6)
  lastBillingDate    DateTime?         @map("last_billing_date") @db.Date
  bills              Bill[]
  subscriptionLogs   SubscriptionLog[]
  clients            Client            @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations      Organization      @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  packages           Package           @relation(fields: [packageId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("subscriptions")
}

model TrainingProgram {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId     String              @map("organization_id") @db.Uuid
  name               String
  description        String?
  scientistId        String?             @map("scientist_id") @db.Uuid
  createdAt          DateTime?           @default(now()) @map("created_at") @db.Timestamptz(6)
  programAssignments ProgramAssignment[]
  organizations      Organization        @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles           Profile?            @relation(fields: [scientistId], references: [id], onUpdate: NoAction)
  workoutdays        WorkoutDay[]

  @@map("trainingprograms")
}

model UhidSequence {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  yearMonth      String       @map("year_month")
  lastSerial     Int          @default(0) @map("last_serial")
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organizationId, yearMonth])
  @@map("uhidsequences")
}

model User {
  id                                        String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email                                     String                     @unique
  passwordHash                              String?                    @map("password_hash")
  role                                      String                     @default("user")
  createdAt                                 DateTime?                  @default(now()) @map("created_at") @db.Timestamptz(6)
  athleteItemLogs                           AthleteItemLog[]
  athleteWorkoutCompletions                 AthleteWorkoutCompletion[]
  audit_logs                                audit_logs[]
  authsessions                              AuthSession[]
  bills                                     Bill[]
  clientAssessmentReports                   ClientAssessmentReport[]
  clientGroupMembers                        ClientGroupMember[]
  clientGroups                              ClientGroup[]
  clientadminnotes                          ClientAdminNote[]
  clientdocuments                           ClientDocument[]
  clientsClientsAssignedConsultantIdTousers Client[]                   @relation("clients_assigned_consultant_idTousers")
  clientsClientsCreatedByTousers            Client[]                   @relation("clients_created_byTousers")
  enquiryinteractions                       EnquiryInteraction[]
  excelDiagnosticReports                    ExcelDiagnosticReport[]
  maxPrRecords                              MaxPrRecord[]
  notificationReads                         NotificationRead[]
  notifications                             Notification[]
  nutritionAssessments                      NutritionAssessment[]
  profiles                                  Profile?
  programAssignments                        ProgramAssignment[]
  questionnaires                            Questionnaire[]
  reportTemplates                           ReportTemplate[]
  scientificResources                       ScientificResource[]
  sessions                                  Session[]
  userAppActivities                         UserAppActivity[]
  wellnessLogs                              WellnessLog[]

  @@map("users")
}

model Waitlist {
  id                String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId    String       @map("organization_id") @db.Uuid
  clientId          String       @map("client_id") @db.Uuid
  therapistId       String?      @map("therapist_id") @db.Uuid
  serviceId         String?      @map("service_id") @db.Uuid
  preferredDate     DateTime     @map("preferred_date") @db.Date
  preferredTimeSlot String?      @map("preferred_time_slot")
  preferenceType    String?      @map("preference_type")
  status            String?      @default("Waiting")
  createdAt         DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime?    @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients           Client       @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations     Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  services          Service?     @relation(fields: [serviceId], references: [id], onUpdate: NoAction)
  profiles          Profile?     @relation(fields: [therapistId], references: [id], onUpdate: NoAction)

  @@map("waitlist")
}

model WellnessLog {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  athleteId      String       @map("athlete_id") @db.Uuid
  sleepScore     Int?         @map("sleep_score")
  stressLevel    Int?         @map("stress_level")
  sorenessLevel  Int?         @map("soreness_level")
  fatigueLevel   Int?         @map("fatigue_level")
  sorenessData   Json?        @map("soreness_data")
  createdAt      DateTime?    @default(now()) @map("created_at") @db.Timestamptz(6)
  users          User         @relation(fields: [athleteId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations  Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("wellness_logs")
}

model WorkoutDay {
  id                        String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  programId                 String                     @map("program_id") @db.Uuid
  organizationId            String?                    @map("organization_id") @db.Uuid
  title                     String
  displayOrder              Int                        @map("display_order")
  createdAt                 DateTime?                  @default(now()) @map("created_at") @db.Timestamptz(6)
  athleteWorkoutCompletions AthleteWorkoutCompletion[]
  organizations             Organization?              @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  trainingprograms          TrainingProgram            @relation(fields: [programId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  workoutitems              WorkoutItem[]

  @@map("workoutdays")
}

model WorkoutItem {
  id              String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workoutDayId    String           @map("workout_day_id") @db.Uuid
  itemType        String           @map("item_type")
  displayOrder    Int              @map("display_order")
  createdAt       DateTime?        @default(now()) @map("created_at") @db.Timestamptz(6)
  athleteItemLogs AthleteItemLog[]
  liftitems       LiftItem[]
  workoutdays     WorkoutDay       @relation(fields: [workoutDayId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("workoutitems")
}

model recurring_questionnaires {
  id               String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id  String        @db.Uuid
  questionnaire_id String        @db.Uuid
  specialist_id    String        @db.Uuid
  client_ids       String[]      @default([]) @db.Uuid
  recurrence_type  String
  status           String        @default("active")
  next_run         DateTime      @default(now()) @db.Timestamptz(6)
  last_run         DateTime?     @db.Timestamptz(6)
  created_at       DateTime?     @default(now()) @db.Timestamptz(6)
  organizations    Organization  @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  questionnaires   Questionnaire @relation(fields: [questionnaire_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles         Profile       @relation(fields: [specialist_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

model NutritionAssessment {
  id                         String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId                   String?       @map("client_id") @db.Uuid
  organizationId             String?       @map("organization_id") @db.Uuid
  nutritionistId             String?       @map("nutritionist_id") @db.Uuid
  name                       String?
  age                        Int?
  gender                     String?
  profession                 String?
  clientType                 String?       @default("athlete") @map("client_type")
  sport                      String?
  position                   String?
  trainingAge                String?       @map("training_age")
  competitionLevel           String?       @map("competition_level")
  exercise                   Boolean?      @default(true)
  exerciseDuration           String?       @map("exercise_duration")
  trainingSessionsCount      Int?          @map("training_sessions_count")
  exerciseType               String?       @map("exercise_type")
  heightCm                   Decimal?      @map("height_cm") @db.Decimal
  weightKg                   Decimal?      @map("weight_kg") @db.Decimal
  bodyFatPct                 Decimal?      @map("body_fat_pct") @db.Decimal
  muscleMassKg               Decimal?      @map("muscle_mass_kg") @db.Decimal
  bmi                        Decimal?      @db.Decimal
  complaints                 String?
  biochemicalInterpretations String?       @map("biochemical_interpretations")
  medicalHistory             String?       @map("medical_history")
  otherMedications           String?       @map("other_medications")
  allergiesIntolerances      Json?         @default("[]") @map("allergies_intolerances")
  dietaryPreference          String?       @default("Non-Vegetarian") @map("dietary_preference")
  sleepDurationHours         Decimal?      @map("sleep_duration_hours") @db.Decimal
  dailyFluidIntakeL          Decimal?      @map("daily_fluid_intake_l") @db.Decimal
  timelineRecall             Json?         @default("{}") @map("timeline_recall")
  session1                   Json?         @default("{}") @map("session_1")
  session2                   Json?         @default("{}") @map("session_2")
  supplements                Json?         @default("[]")
  observations               String?
  goal                       String?
  advicePrescription         String?       @map("advice_prescription")
  takenBy                    String?       @map("taken_by")
  assessmentDate             DateTime?     @default(now()) @map("assessment_date") @db.Timestamptz(6)
  createdAt                  DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                  DateTime?     @default(now()) @map("updated_at") @db.Timestamptz(6)
  clients                    Client?       @relation(fields: [clientId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles                   User?         @relation(fields: [nutritionistId], references: [id], onUpdate: NoAction)
  organizations              Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("nutrition_assessments")
}

model UserAppActivity {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId        String    @map("user_id") @db.Uuid
  date          DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  activeSeconds Int       @default(0) @map("active_seconds")
  lastPing      DateTime? @default(now()) @map("last_ping") @db.Timestamptz(6)
  users         User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([userId, date], map: "unique_user_date")
  @@map("user_app_activity")
}

model ActivityTrackerAutomation {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId  String?       @map("organization_id") @db.Uuid
  isEnabled       Boolean?      @default(false) @map("is_enabled")
  recipientEmails String        @default("") @map("recipient_emails")
  scheduledTime   String        @default("18:00") @map("scheduled_time")
  lastSentDate    DateTime?     @map("last_sent_date") @db.Date
  createdAt       DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime?     @default(now()) @map("updated_at") @db.Timestamptz(6)
  organizations   Organization? @relation(fields: [organizationId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("activity_tracker_automation")
}

model audit_logs {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id String?       @db.Uuid
  entity_type     String
  entity_id       String        @db.Uuid
  action          String
  performed_by    String?       @db.Uuid
  details         Json?         @default("{}")
  created_at      DateTime?     @default(now()) @db.Timestamptz(6)
  organizations   Organization? @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users           User?         @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
}

model hr_leave_balances {
  id              String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id String       @db.Uuid
  employee_id     String       @db.Uuid
  casual_leave    Int?         @default(12)
  sick_leave      Int?         @default(4)
  paid_leave      Int?         @default(0)
  emergency_leave Int?         @default(0)
  created_at      DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?    @default(now()) @db.Timestamptz(6)
  profiles        Profile      @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations   Organization @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organization_id, employee_id], map: "unique_org_employee_leave_balance")
}

model UserOrganization {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId         String    @map("user_id") @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  role           String    @default("member")
  joinedAt       DateTime  @default(now()) @map("joined_at") @db.Timestamptz(6)
  invitedBy      String?   @map("invited_by") @db.Uuid

  @@unique([userId, organizationId])
  @@index([userId], map: "idx_user_organizations_user")
  @@index([organizationId], map: "idx_user_organizations_org")
  @@map("user_organizations")
}

model ChatBot {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  name           String    @default("HubBot")
  avatarUrl      String?   @map("avatar_url")
  description    String?
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)

  @@unique([organizationId, name])
  @@index([organizationId], map: "idx_chat_bots_org")
  @@map("chat_bots")
}

model ChatChannel {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId  String    @map("organization_id") @db.Uuid
  name            String
  description     String?
  channelType     String    @default("public") @map("channel_type")
  createdBy       String?   @map("created_by") @db.Uuid
  isArchived      Boolean   @default(false) @map("is_archived")
  isDefault       Boolean   @default(false) @map("is_default")
  lastMessageAt   DateTime? @map("last_message_at") @db.Timestamptz(6)
  createdAt       DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime  @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt       DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@unique([organizationId, name])
  @@index([organizationId], map: "idx_chat_channels_org")
  @@index([channelType], map: "idx_chat_channels_type")
  @@map("chat_channels")
}

model ChannelMember {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  channelId    String    @map("channel_id") @db.Uuid
  userId       String    @map("user_id") @db.Uuid
  role         String    @default("member")
  muted        Boolean   @default(false)
  joinedAt     DateTime  @default(now()) @map("joined_at") @db.Timestamptz(6)
  lastReadAt   DateTime? @map("last_read_at") @db.Timestamptz(6)

  @@unique([channelId, userId])
  @@index([channelId], map: "idx_channel_members_channel")
  @@index([userId], map: "idx_channel_members_user")
  @@map("channel_members")
}

model DirectMessageThread {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  userA          String    @map("user_a") @db.Uuid
  userB          String    @map("user_b") @db.Uuid
  lastMessageAt  DateTime? @map("last_message_at") @db.Timestamptz(6)
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)

  @@unique([organizationId, userA, userB])
  @@index([userA], map: "idx_dm_threads_user_a")
  @@index([userB], map: "idx_dm_threads_user_b")
  @@index([organizationId], map: "idx_dm_threads_org")
  @@map("direct_message_threads")
}

model ChatMessage {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId    String    @map("organization_id") @db.Uuid
  channelId         String?   @map("channel_id") @db.Uuid
  dmThreadId        String?   @map("dm_thread_id") @db.Uuid
  userId            String?   @map("user_id") @db.Uuid
  botId             String?   @map("bot_id") @db.Uuid
  parentMessageId   String?   @map("parent_message_id") @db.Uuid
  messageType       String    @default("user") @map("message_type")
  content           String?
  contentHtml       String?   @map("content_html")
  metadata          Json?
  isEdited          Boolean   @default(false) @map("is_edited")
  editedAt          DateTime? @map("edited_at") @db.Timestamptz(6)
  createdAt         DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime  @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt         DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@index([channelId, createdAt(sort: Desc)], map: "idx_chat_messages_channel")
  @@index([dmThreadId, createdAt(sort: Desc)], map: "idx_chat_messages_dm")
  @@index([parentMessageId], map: "idx_chat_messages_thread")
  @@index([organizationId, createdAt(sort: Desc)], map: "idx_chat_messages_org")
  @@map("chat_messages")
}

model MessageAttachment {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  messageId  String   @map("message_id") @db.Uuid
  fileName   String   @map("file_name")
  fileUrl    String   @map("file_url")
  fileSize   BigInt?  @map("file_size")
  mimeType   String?  @map("mime_type")
  uploadedBy String?  @map("uploaded_by") @db.Uuid
  uploadedAt DateTime @default(now()) @map("uploaded_at") @db.Timestamptz(6)

  @@index([messageId], map: "idx_attachments_message")
  @@map("message_attachments")
}

model MessageReaction {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  messageId String   @map("message_id") @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  emoji     String
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)

  @@unique([messageId, userId, emoji])
  @@index([messageId], map: "idx_reactions_message")
  @@map("message_reactions")
}

model MessageRead {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  messageId String   @map("message_id") @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  readAt    DateTime @default(now()) @map("read_at") @db.Timestamptz(6)

  @@unique([messageId, userId])
  @@index([userId], map: "idx_reads_user")
  @@index([messageId], map: "idx_reads_message")
  @@map("message_reads")
}

model TeamcommsSettings {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId         String    @unique @map("organization_id") @db.Uuid
  isEnabled              Boolean   @default(true) @map("is_enabled")
  notifyTaskAssigned     Boolean   @default(true) @map("notify_task_assigned")
  notifyTaskOverdue      Boolean   @default(true) @map("notify_task_overdue")
  notifyAppointment      Boolean   @default(true) @map("notify_appointment")
  notifyLeave            Boolean   @default(true) @map("notify_leave")
  notifyClinicalReport   Boolean   @default(true) @map("notify_clinical_report")
  notifyMealPlan         Boolean   @default(true) @map("notify_meal_plan")
  notifyMembershipExpiry Boolean   @default(true) @map("notify_membership_expiry")
  reportChannelId        String?   @map("report_channel_id") @db.Uuid
  createdAt              DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt              DateTime  @default(now()) @map("updated_at") @db.Timestamptz(6)

  @@map("teamcomms_settings")
}

model TeamcommsScheduledReport {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  channelId      String    @map("channel_id") @db.Uuid
  reportType     String    @map("report_type")
  cronExpression String    @map("cron_expression")
  isActive       Boolean   @default(true) @map("is_active")
  lastRunAt      DateTime? @map("last_run_at") @db.Timestamptz(6)
  createdBy      String?   @map("created_by") @db.Uuid
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)

  @@index([organizationId], map: "idx_scheduled_reports_org")
  @@map("teamcomms_scheduled_reports")
}

model PlannerProject {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organizationId        String?                @map("organization_id") @db.Uuid
  name                  String
  code                  String
  description           String?
  department            String?                @default("General")
  priority              String?                @default("medium")
  health                String?                @default("on_track")
  status                String?                @default("active")
  progress              Int?                   @default(0)
  startDate             DateTime?              @map("start_date") @db.Date
  targetDate            DateTime?              @map("target_date") @db.Date
  budget                Decimal?               @default(0.00) @db.Decimal(15, 2)
  currency              String?                @default("INR")
  ownerId               String?                @map("owner_id") @db.Uuid
  managerId             String?                @map("manager_id") @db.Uuid
  portfolioId           String?                @map("portfolio_id") @db.Uuid
  createdAt             DateTime?              @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt             DateTime?              @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt             DateTime?              @map("deleted_at") @db.Timestamptz(6)
  plannerWorkstreams    PlannerWorkstream[]
  plannerWorkItems      PlannerWorkItem[]

  @@map("planner_projects")
}

model PlannerWorkstream {
  id                  String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  projectId           String?                @map("project_id") @db.Uuid
  name                String
  color               String?                @default("hsl(251 74% 60%)")
  sortOrder           Int?                   @default(0) @map("sort_order")
  createdAt           DateTime?              @default(now()) @map("created_at") @db.Timestamptz(6)
  plannerProject      PlannerProject?        @relation(fields: [projectId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  plannerWorkItems    PlannerWorkItem[]

  @@map("planner_workstreams")
}

model PlannerWorkItem {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  projectId             String?                @map("project_id") @db.Uuid
  workstreamId          String?                @map("workstream_id") @db.Uuid
  parentId              String?                @map("parent_id") @db.Uuid
  title                 String
  description           String?
  status                String?                @default("planned")
  priority              String?                @default("medium")
  assigneeId            String?                @map("assignee_id") @db.Uuid
  creatorId             String?                @map("creator_id") @db.Uuid
  sprintId              String?                @map("sprint_id") @db.Uuid
  startDate             DateTime?              @map("start_date") @db.Date
  dueDate               DateTime?              @map("due_date") @db.Date
  estimatedHours        Decimal?               @default(0.00) @map("estimated_hours") @db.Decimal(8, 2)
  actualHours           Decimal?               @default(0.00) @map("actual_hours") @db.Decimal(8, 2)
  isMilestone           Boolean?               @default(false) @map("is_milestone")
  isCritical            Boolean?               @default(false) @map("is_critical")
  sortOrder             Int?                   @default(0) @map("sort_order")
  createdAt             DateTime?              @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt             DateTime?              @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt             DateTime?              @map("deleted_at") @db.Timestamptz(6)
  plannerProject        PlannerProject?        @relation(fields: [projectId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  plannerWorkstream     PlannerWorkstream?     @relation(fields: [workstreamId], references: [id], onUpdate: NoAction)
  parent                PlannerWorkItem?       @relation("WorkItemHierarchy", fields: [parentId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  children              PlannerWorkItem[]      @relation("WorkItemHierarchy")
  predecessorDeps       PlannerDependency[]    @relation("WorkItemPredecessor")
  successorDeps         PlannerDependency[]    @relation("WorkItemSuccessor")

  @@map("planner_work_items")
}

model PlannerDependency {
  id              String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  predecessorId   String           @map("predecessor_id") @db.Uuid
  successorId     String           @map("successor_id") @db.Uuid
  type            String?          @default("FS")
  lagDays         Int?             @default(0) @map("lag_days")
  createdAt       DateTime?        @default(now()) @map("created_at") @db.Timestamptz(6)
  predecessor     PlannerWorkItem  @relation("WorkItemPredecessor", fields: [predecessorId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  successor       PlannerWorkItem  @relation("WorkItemSuccessor", fields: [successorId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([predecessorId, successorId])
  @@map("planner_dependencies")
}

model PlannerDailyTask {
  id               String    @id
  organizationId   String?   @map("organization_id") @db.Uuid
  title            String
  description      String?
  date             String
  timeMode         String?   @map("time_mode")
  startTime        String?   @map("start_time")
  endTime          String?   @map("end_time")
  hasTimeSlot      Boolean?  @default(false) @map("has_time_slot")
  isSetTime        Boolean?  @default(false) @map("is_set_time")
  deadline         String?
  deadlineTime     String?   @map("deadline_time")
  category         String?   @default("other")
  priority         String?   @default("medium")
  status           String?   @default("scheduled")
  taskType         String?   @default("individual") @map("task_type")
  assignerId       String?   @map("assigner_id")
  assignerName     String?   @map("assigner_name")
  assigneeId       String?   @map("assignee_id")
  assigneeName     String?   @map("assignee_name")
  teamId           String?   @map("team_id")
  teamName         String?   @map("team_name")
  creatorId        String?   @map("creator_id")
  creatorName      String?   @map("creator_name")
  requiresApproval Boolean?  @default(false) @map("requires_approval")
  approverId       String?   @map("approver_id")
  approverName     String?   @map("approver_name")
  approvalStatus   String?   @map("approval_status")
  approvalNote     String?   @map("approval_note")
  rejectionReason  String?   @map("rejection_reason")
  reviewedAt       String?   @map("reviewed_at")
  completedAt      String?   @map("completed_at")
  createdAt        DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt        DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@map("planner_daily_tasks")
}

model PlannerTeam {
  id             String    @id
  organizationId String?   @map("organization_id") @db.Uuid
  name           String
  code           String
  department     String
  description    String?
  color          String?
  leadId         String?   @map("lead_id")
  leadName       String?   @map("lead_name")
  memberIds      Json?     @default("[]") @map("member_ids")
  createdAt      DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt      DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt      DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@map("planner_teams")
}

model PlannerSetting {
  id             String    @id
  organizationId String?   @map("organization_id") @db.Uuid
  settings       Json
  updatedAt      DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)

  @@map("planner_settings")
}
