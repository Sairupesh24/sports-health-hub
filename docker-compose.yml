services:
  postgres:
    image: postgres:18-alpine
    container_name: ishpo-postgres
    environment:
      POSTGRES_USER: skavuturi
      POSTGRES_PASSWORD: Ksr24rupesh
      POSTGRES_DB: ishpo
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql
    restart: unless-stopped

volumes:
  pgdata:
